name: Urgente padroniza motorista

on:
  push:
    paths:
      - '.github/workflows/urgente-padroniza-motorista.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  corrigir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Corrigir agenda e leitura
        shell: python
        run: |
          from pathlib import Path

          agenda = Path('timoni-portal/app/dashboard/motorista/motorista-agenda.tsx')
          s = agenda.read_text(encoding='utf-8')

          # Tipo e estado da lista de vendedores do Estoque.
          s = s.replace('type Modo = "dia" | "semana" | "mes";\n', 'type Modo = "dia" | "semana" | "mes";\ntype Seller = { nome: string; unidade: string };\n', 1)
          s = s.replace('  const [invalidos, setInvalidos] = useState<Set<keyof FormState>>(new Set());\n', '  const [invalidos, setInvalidos] = useState<Set<keyof FormState>>(new Set());\n  const [sellers, setSellers] = useState<Seller[]>([]);\n', 1)
          state_anchor = '  const diasSemana = useMemo(() => weekDays(selecionado), [selecionado]);\n'
          seller_block = '''  const sellerOptions = useMemo(
    () => sellers.filter((item) => !item.unidade || item.unidade === form.loja || item.unidade === "todas"),
    [sellers, form.loja],
  );

  useEffect(() => {
    void (async () => {
      try {
        const response = await fetch("/api/estoque", { cache: "no-store" });
        const payload = await response.json();
        if (response.ok && payload?.ok && Array.isArray(payload.vendedores)) setSellers(payload.vendedores);
      } catch {}
    })();
  }, []);
'''
          if 'const sellerOptions = useMemo(' not in s:
            s = s.replace(state_anchor, state_anchor + '\n' + seller_block, 1)

          # Helpers de endereço e pedido.
          helper_anchor = '''function separarEndereco(endereco?: string) {
  const valor = endereco || "";
  const match = valor.match(/\\nLink:\\s*(https?:\\/\\/\\S+)/i);
  return {
    texto: valor.replace(/\\nLink:\\s*https?:\\/\\/\\S+/i, "").trim(),
    link: match?.[1] || "",
  };
}
'''
          helper_extra = '''
function formatarEnderecoExibicao(endereco?: string, numero?: string, complemento?: string) {
  const info = separarEndereco(endereco);
  const num = (numero || "").trim();
  let partes = info.texto.split(/\\s+-\\s+/).map((parte) => parte.trim()).filter(Boolean);
  if (num) {
    const escaped = num.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&");
    const fimNumero = new RegExp(`(?:\\\\s*[-,]?\\\\s*${escaped})+$`);
    partes = partes
      .map((parte) => parte === num ? "" : parte.replace(fimNumero, "").replace(/[\\s,-]+$/, "").trim())
      .filter(Boolean);
  }
  const rua = partes.shift() || "";
  const inicio = num && rua ? `${rua}, ${num}` : rua || num;
  return [inicio, ...partes, complemento].filter(Boolean).join(" - ");
}

function limparEnderecoFormulario(endereco?: string, numero?: string) {
  return formatarEnderecoExibicao(endereco, numero).replace(numero ? `, ${numero}` : "", "").trim();
}

function pedidoTexto(valor?: string) {
  return (valor || "").replace(/\\s*\\/\\s*/g, " ").replace(/\\s+/g, " ").trim();
}
'''
          if 'function formatarEnderecoExibicao(' not in s:
            if helper_anchor not in s:
              raise SystemExit('helper endereco agenda nao encontrado')
            s = s.replace(helper_anchor, helper_anchor + helper_extra, 1)

          # Edição não concatena número de novo e recupera CEP quando já salvo.
          s = s.replace('  const enderecoCompleto = [v.endereco, v.numero, v.complemento].filter(Boolean).join(" - ");\n  const match = enderecoCompleto.match(/\\nLink:\\s*(https?:\\/\\/\\S+)/i);', '  const enderecoBase = v.endereco || "";\n  const match = enderecoBase.match(/\\nLink:\\s*(https?:\\/\\/\\S+)/i);\n  const enderecoLimpo = limparEnderecoFormulario(enderecoBase, v.numero);\n  const cepEncontrado = enderecoLimpo.match(/\\b\\d{5}-?\\d{3}\\b/)?.[0] || "";', 1)
          s = s.replace('    cep: "",\n    endereco: enderecoCompleto.replace(/\\nLink:\\s*https?:\\/\\/\\S+/i, "").trim(),', '    cep: cepEncontrado,\n    endereco: enderecoLimpo.replace(/\\nLink:\\s*https?:\\/\\/\\S+/i, "").trim(),', 1)

          # CEP entra no endereço; link só é gerado após número.
          old_cep = '''      const endereco = [data.logradouro, data.bairro, [data.localidade, data.uf].filter(Boolean).join("/")].filter(Boolean).join(" - ");
      const linkMaps = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(endereco)}`;
      set("endereco", endereco);
      set("numeroEndereco", "");
      set("linkEndereco", "");
      setErro("CEP localizado. Confirme o número do endereço para gerar o link do Google Maps.");
      set("linkEndereco", linkMaps);'''
          new_cep = '''      const endereco = [data.logradouro, data.bairro, [data.localidade, data.uf].filter(Boolean).join("/"), formatarCep(cep)].filter(Boolean).join(" - ");
      set("endereco", endereco);
      set("numeroEndereco", "");
      set("linkEndereco", "");
      setErro("CEP localizado. Confirme o número do endereço para gerar o link do Google Maps.");'''
          if old_cep in s:
            s = s.replace(old_cep, new_cep, 1)

          # Sempre salva link Maps com endereço + número atuais.
          s = s.replace('      const linkMaps = form.linkEndereco || `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(enderecoCompleto)}`;', '      const linkMaps = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(enderecoCompleto)}`;', 1)

          # Lista de vendedores idêntica ao Estoque e dependente da loja.
          old_seller = '<label className="text-sm font-medium text-slate-700">* Vendedor<select value={form.vendedor} onChange={(e) => set("vendedor", e.target.value)} className={fieldClass("vendedor")}><option value="">Selecione</option>{form.vendedor && !AUTORIZADOS.includes(form.vendedor as (typeof AUTORIZADOS)[number]) && <option value={form.vendedor}>{form.vendedor}</option>}{AUTORIZADOS.map((nome) => <option key={nome} value={nome}>{nome}</option>)}</select></label>'
          new_seller = '<label className="text-sm font-medium text-slate-700">* Vendedor<select value={form.vendedor} onChange={(e) => set("vendedor", e.target.value)} className={fieldClass("vendedor")}><option value="">Selecione</option>{form.vendedor && !sellerOptions.some((item) => item.nome === form.vendedor) && <option value={form.vendedor}>{form.vendedor}</option>}{sellerOptions.map((item) => <option key={`${item.nome}-${item.unidade}`} value={item.nome}>{item.nome}</option>)}</select></label>'
          if old_seller in s:
            s = s.replace(old_seller, new_seller, 1)
          s = s.replace('<label className="text-sm font-medium text-slate-700">* Loja<select value={form.loja} onChange={(e) => set("loja", e.target.value)}', '<label className="text-sm font-medium text-slate-700">* Loja<select value={form.loja} onChange={(e) => { set("loja", e.target.value); set("vendedor", ""); }}', 1)

          # Card semanal completo no padrão solicitado.
          old_week = '''                      <button key={v.id} type="button" onClick={() => abrirEditar(v)} className="block w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-left hover:bg-slate-100">
                        <p className="text-sm font-semibold text-slate-950">{index + 1}. {lojaLabel(v.loja)}{v.vendedor ? ` | Vend.: ${v.vendedor}` : ""}{v.horario ? ` | ${horaCurta(v.horario)}` : ""}</p>
                        {v.clienteFornecedor && <p className="mt-1 truncate text-xs text-slate-600">{v.clienteFornecedor}</p>}
                      </button>'''
          new_week = '''                      <button key={v.id} type="button" onClick={() => abrirEditar(v)} className="block w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-left hover:bg-slate-100">
                        <p className="text-sm font-semibold text-slate-950">{index + 1}. {lojaLabel(v.loja)}{v.vendedor ? ` | Vend.: ${v.vendedor}` : ""}{v.horario ? ` | ${horaCurta(v.horario)}` : ""}</p>
                        {v.tipoHorario !== "Bloqueio" && <>
                          <p className="mt-2 text-sm font-medium text-slate-900">{v.clienteFornecedor || ""}{pedidoTexto(v.numeroPedido) ? ` | ${pedidoTexto(v.numeroPedido)}` : ""}{v.volumes ? ` | Volume: ${v.volumes}` : ""}</p>
                          {v.endereco && <p className="mt-1 text-sm text-slate-700">{formatarEnderecoExibicao(v.endereco, v.numero, v.complemento)}</p>}
                          {separarEndereco(v.endereco).link && <a href={separarEndereco(v.endereco).link} onClick={(e) => e.stopPropagation()} target="_blank" rel="noreferrer" className="mt-2 inline-flex text-xs font-semibold text-blue-800 underline">Abrir no Google Maps</a>}
                          {(v.contatoNome || v.contatoWhats) && <p className="mt-2 text-sm text-slate-700">Contato: {[v.contatoNome, v.contatoWhats].filter(Boolean).join(" - ")}</p>}
                        </>}
                        {v.info && <p className="mt-2 text-sm text-slate-600">Observação: {v.info}</p>}
                        {v.preenchidoPor && <p className="mt-2 text-[10px] text-slate-500">Preenchido: {v.preenchidoPor}</p>}
                      </button>'''
          if old_week in s:
            s = s.replace(old_week, new_week, 1)

          # Card detalhado do dia.
          s = s.replace('<p className="mt-3 text-slate-900">{v.clienteFornecedor || ""}{v.numeroPedido ? ` · ${v.numeroPedido}` : ""}{v.volumes ? ` · Volume: ${v.volumes}` : ""}</p>', '<p className="mt-3 text-slate-900">{v.clienteFornecedor || ""}{pedidoTexto(v.numeroPedido) ? ` | ${pedidoTexto(v.numeroPedido)}` : ""}{v.volumes ? ` | Volume: ${v.volumes}` : ""}</p>', 1)
          s = s.replace('{[separarEndereco(v.endereco).texto, v.numero, v.complemento].filter(Boolean).join(" - ")}', '{formatarEnderecoExibicao(v.endereco, v.numero, v.complemento)}', 1)

          agenda.write_text(s, encoding='utf-8')

          # Leitura do motorista.
          leitura = Path('timoni-portal/app/motorista/motorista-leitura.tsx')
          t = leitura.read_text(encoding='utf-8')
          helper_anchor2 = '''function separarEndereco(endereco?: string) {
  const valor = endereco || "";
  const match = valor.match(/\\nLink:\\s*(https?:\\/\\/\\S+)/i);
  return {
    texto: valor.replace(/\\nLink:\\s*https?:\\/\\/\\S+/i, "").trim(),
    link: match?.[1] || "",
  };
}
'''
          helper_extra2 = '''
function formatarEnderecoExibicao(endereco?: string, numero?: string, complemento?: string) {
  const info = separarEndereco(endereco);
  const num = (numero || "").trim();
  let partes = info.texto.split(/\\s+-\\s+/).map((parte) => parte.trim()).filter(Boolean);
  if (num) {
    const escaped = num.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&");
    const fimNumero = new RegExp(`(?:\\\\s*[-,]?\\\\s*${escaped})+$`);
    partes = partes.map((parte) => parte === num ? "" : parte.replace(fimNumero, "").replace(/[\\s,-]+$/, "").trim()).filter(Boolean);
  }
  const rua = partes.shift() || "";
  const inicio = num && rua ? `${rua}, ${num}` : rua || num;
  return [inicio, ...partes, complemento].filter(Boolean).join(" - ");
}

function pedidoTexto(valor?: string) {
  return (valor || "").replace(/\\s*\\/\\s*/g, " ").replace(/\\s+/g, " ").trim();
}
'''
          if 'function formatarEnderecoExibicao(' not in t:
            if helper_anchor2 not in t:
              raise SystemExit('helper endereco leitura nao encontrado')
            t = t.replace(helper_anchor2, helper_anchor2 + helper_extra2, 1)
          t = t.replace('                      const endereco = [enderecoInfo.texto, v.numero, v.complemento].filter(Boolean).join(" - ");', '                      const endereco = formatarEnderecoExibicao(v.endereco, v.numero, v.complemento);', 1)
          t = t.replace('<p className="mt-2 text-sm font-medium text-slate-900">{v.clienteFornecedor || ""} {v.numeroPedido || ""} {v.volumes ? `Volume: ${v.volumes}` : "Volume:"}</p>', '<p className="mt-2 text-sm font-medium text-slate-900">{v.clienteFornecedor || ""}{pedidoTexto(v.numeroPedido) ? ` | ${pedidoTexto(v.numeroPedido)}` : ""}{v.volumes ? ` | Volume: ${v.volumes}` : ""}</p>', 1)
          t = t.replace('Preenchido por: {v.preenchidoPor}', 'Preenchido: {v.preenchidoPor}', 1)
          leitura.write_text(t, encoding='utf-8')

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add timoni-portal/app/dashboard/motorista/motorista-agenda.tsx timoni-portal/app/motorista/motorista-leitura.tsx
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix: padroniza cards endereco e vendedores do motorista"
          git push
